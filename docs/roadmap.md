# cclaw Roadmap

Updated: 2026-04-17

## Direction

Short term: finish Phase 1-3 implementation (integrity, cleanup, git-native isolation) — done in v0.16–v0.21.2.

Long term: build eval-first discipline (scenario replay, regression scoring, trace-driven tuning). See Phase 7 below for the full staged plan from zero to multi-stage e2e evals.

## Phase 1 - Integrity (v0.16)

Goals:

- Enforce real TDD subagent dispatch in TDD stage.
- Add deterministic cross-stage traceability from scope decisions to plan and TDD evidence.
- Prevent placeholder and scope-shrinking plans.
- Lock event schemas now to avoid future migrations.

Scope:

- Split TDD into:
  - subagent body skill (`test-driven-development`)
  - controller runbook (`06-tdd`)
- Add locked decision IDs (`D-XX`) in scope artifacts.
- Add plan lint rules:
  - no placeholders (`TBD`, `TODO`, "similar to task", etc.)
  - no scope-reduction phrases (`v1`, `static for now`, etc.) when locked decisions exist
- Expand log schemas:
  - delegation log spans (start/end/status/tokens/retries/evidence refs)
  - stage activity schema version
- Expand knowledge entries with origin/frequency/maturity fields.
- Add artifact frontmatter hashes for replay integrity.

Exit criteria:

- TDD mandatory delegation is observable in delegation logs.
- Artifact linter catches placeholders and locked-decision drift.
- New log and knowledge schemas are emitted in all new sessions.

## Phase 2 - Cleanup (v0.17)

Goals:

- Keep workflow power, remove low-value runtime complexity.

Scope:

- Remove preamble runtime tracking file and related logic.
- Remove gate condition DSL and keep explicit prose rules.
- Consolidate anti-pattern/rationalization sections to reduce duplication.
- Simplify command surface (`rewind` + `rewind-ack` into one confirmed action).
- Shrink oversized content modules by moving long examples to references.

Exit criteria:

- Test/build suite remains green.
- LOC in core runtime/content drops significantly without behavior regression.

## Phase 3 - Git-Native Isolation (v0.18)

Goals:

- Replace custom run workspace implementation with git worktrees.

Scope:

- Introduce `using-git-worktrees` guidance and runtime path.
- Migrate new feature execution to git worktrees.
- Keep backward compatibility for old workspace artifacts (read-only migration path).
- Remove old run/feature system code after migration is stable.

Exit criteria:

- New feature runs use worktrees by default.
- Legacy runs still readable for audit/history.
- CI/tests pass with old runtime removed.

## Phase 7 - Eval-Driven Prompt Engineering (v0.22 - v0.28)

North star: skill and prompt changes are merged based on measured score deltas, not subjective judgment.

### Locked Decisions

- **D-EVAL-01**: LLM client = official `openai` npm SDK with custom `baseURL`. Zero vendor lock-in; any OpenAI-compatible endpoint works (z.ai, OpenAI, together.ai, self-hosted vLLM, etc.).
- **D-EVAL-02**: Configuration via environment variables only. No secrets in repo.
  - `CCLAW_EVAL_API_KEY` (required once the LLM judge step ships)
  - `CCLAW_EVAL_BASE_URL` (default `https://api.z.ai/api/coding/paas/v4`)
  - `CCLAW_EVAL_MODEL` (default `glm-5.1`)
  - `CCLAW_EVAL_JUDGE_MODEL` (default = `CCLAW_EVAL_MODEL`)
  - `CCLAW_EVAL_DAILY_USD_CAP` (default unset = no cap; explicit opt-in)
- **D-EVAL-03**: Tier C in this phase = multi-stage workflow run via SDK with function-calling. Real IDE harness integration (claude-code / cursor-agent through a GLM proxy) is **deferred to Phase 8**.
- **D-EVAL-04**: Two model roles — Agent-under-test (AUT) and Judge. Both default to the same model; `CCLAW_EVAL_JUDGE_MODEL` allows cross-model judging for research.
- **D-EVAL-05**: Each step ships as its own PR + version bump. Steps are additive and independently valuable.

### Step 0 — Foundations (v0.22.0)

**Goals:** skeleton for `cclaw eval` CLI and directory layout. Zero LLM calls.

Scope:

- `.cclaw/evals/{config.yaml,corpus/,rubrics/,baselines/,reports/}` scaffolding generated by `cclaw init`.
- `src/eval/{runner.ts,types.ts,llm-client.ts,report.ts}` skeleton.
- `cclaw eval --help`, `cclaw eval --dry-run` (validates config + empty corpus).
- `docs/evals.md` with rationale and usage.

Exit criteria:

- `cclaw eval --dry-run` exits 0 with empty corpus.
- Existing 250+ test suite stays green.
- Typecheck clean.

### Step 1 — Schema Verifiers L1 (v0.23.0) — shipped

**Goals:** first regression layer, still zero LLM. Runs on every PR.

Scope:

- `src/eval/verifiers/structural.ts` — checks required sections, forbidden
  patterns, length bounds (lines/chars, frontmatter excluded), and required
  YAML frontmatter keys.
- Fixture-based corpus in `tests/fixtures/eval-demo/.cclaw/evals/corpus/<stage>/<id>.yaml`
  with a sibling `<id>/fixture.md` as the artifact-under-test.
- 3 cases per stage × 8 stages = 24 fixtures (across dark-mode, auth-refactor,
  and billing-rewrite scenarios) with committed per-stage baselines.
- `src/eval/baseline.ts` — load/write/compare baselines (`baselines/<stage>.json`).
  Writes are gated behind `--update-baseline --confirm` and refuse to run if any
  case is currently failing.
- `cclaw eval --schema-only` runs the real structural verifier.
- Exit code is `1` whenever any case fails *or* any baseline-tracked verifier
  regresses from `ok:true` to `ok:false`.
- `.github/workflows/evals-structural.yml` — PR-blocking, zero-secret CI that
  executes the demo corpus against its committed baselines.

Exit criteria:

- 24 corpus cases pass on main (verified by `tests/integration/eval-structural.test.ts`).
- Synthetic regression (removed required section, injected `TBD` pattern)
  is caught with exit code 1 and surfaces a `newly-failing` entry in the
  baseline delta.

### Step 2 — Rule-based Verifiers L2 (v0.24.0)

**Goals:** content rules without LLM — catches ~60% of regressions cheaply.

Scope:

- `src/eval/verifiers/rules.ts` — keyword/regex must-contain, must-not-contain, numeric bounds (min decisions, min rationalizations), uniqueness.
- `src/eval/verifiers/traceability.ts` — cross-stage pass: D-XX decisions from scope appear in plan and tdd.
- Corpus expanded to 5 cases per stage (40 total).
- `cclaw eval --rules` runs schema + rules.

Exit criteria:

- Each stage covered by ≥3 rule checks.
- Traceability check catches artificial regression in test.
- `cclaw eval --rules` wired into PR CI.

### Step 3 — LLM Judge + Tier A (v0.25.0)

**First step with real API calls to the configured OpenAI-compat endpoint.**

Scope:

- `src/eval/llm-client.ts` with retry, timeout, rate-limit, structured error types.
- `src/eval/cost-guard.ts` — USD counter based on `usage.total_tokens × pricing`; hard-stop when `CCLAW_EVAL_DAILY_USD_CAP` is set and exceeded.
- `src/eval/agents/single-shot.ts` (Tier A) — one API call with SKILL.md as system, case prompt as user.
- `src/eval/verifiers/judge.ts` — median-of-3 scoring, structured JSON output, judge-agreement metric.
- `.cclaw/evals/rubrics/<stage>.yaml` — 3–5 rubric checks per stage.
- Regression thresholds in `config.yaml`:
  - `fail_if_delta_below: -0.15` (>15% drop is a fail)
  - `fail_if_critical_below: 3.0` (any check <3.0 is a fail)
- `.github/workflows/evals-nightly.yml` — cron on main, uses `secrets.CCLAW_EVAL_API_KEY`.
- `cclaw eval --judge --tier=A`.

Exit criteria:

- One-stage judge run completes in <90s and costs <$0.50 per stage.
- Nightly CI runs all 8 stages, produces markdown report artifact.
- Synthetically degraded skill is correctly flagged as regression.
- Cost cap, when enabled, aborts a run mid-flight with a clear error.

### Step 4 — Tier B: Agent with Tools (v0.26.0)

**Goals:** realistic multi-turn runs — AUT reads project files, writes artifacts, uses function-calling.

Scope:

- `src/eval/tools/{read,write,glob,grep}.ts` — sandbox-confined tool implementations with JSON schemas.
- `src/eval/sandbox.ts` — per-case tmp directory (`os.tmpdir()/cclaw-eval-<uuid>/`) with deterministic cleanup.
- `src/eval/agents/with-tools.ts` (Tier B) — multi-turn loop with `tool_choice: "auto"`, `MAX_TURNS` guard.
- Corpus cases gain `context_files` field (real project files copied to sandbox).
- `cclaw eval --tier=B`.
- Tool-use metrics in report: number of tool calls, depth, error rate, rejected paths.

Exit criteria:

- AUT successfully reads `.cclaw/skills/*/SKILL.md` from sandbox, produces a valid artifact.
- Sandbox cleanup verified by test (no leftover dirs after suite).
- Single stage Tier B run completes in <3 min and costs <$3.
- Tool calls cannot escape the sandbox (test with `../etc/passwd` attempt).

### Step 5 — Tier C: End-to-End Workflow (v0.27.0)

**Goals:** multi-stage workflow run — AUT simulates a real user session from brainstorm through plan.

Scope:

- `src/eval/agents/workflow.ts` — orchestrates `brainstorm → scope → design → spec → plan` in sequence, threading prior artifacts as context for the next stage.
- `src/eval/verifiers/workflow-consistency.ts` — cross-artifact checks: decisions flow through, no contradictions, placeholder-free, traceability intact.
- `.cclaw/evals/corpus/workflows/` — 3 full scenarios for MVP: feature-addition, bugfix, refactor.
- `cclaw eval --tier=C`.
- `.github/workflows/evals-release.yml` — runs only on `v*` tag push; posts report to the release notes draft.
- `cclaw eval diff <old-version> <new-version>` — side-by-side report comparison.

Exit criteria:

- Full e2e run of one workflow case completes in <15 min and costs <$5.
- Report shows per-stage scores + cross-stage consistency.
- 3 workflow cases pass consistently on main.

### Step 6 — Polish & Observability (v0.28.0)

Scope:

- HTML report with dark theme, baseline diff, per-stage drill-down, publishable as GitHub Pages artifact.
- Cross-model comparison: run same corpus against an alternate `CCLAW_EVAL_MODEL` (e.g., `gpt-5`, `claude-sonnet-4-5`) and diff results.
- Baseline signing: SHA-256 + timestamps committed to git; `cclaw eval --update-baseline` requires `--confirm` flag.
- `cclaw eval --init` interactive setup (writes `.env`, generates first corpus case).
- Final `docs/evals.md` guide + README quickstart.

Exit criteria:

- HTML report renders locally and in CI.
- Cross-model diff works with one command swap.
- Docs cover setup, authoring cases, authoring rubrics, troubleshooting.

### Deferred to Phase 8+

- Real IDE harness integration (running actual claude-code / cursor-agent sessions against GLM via a proxy layer) — requires adapter between Anthropic/Cursor wire formats and OpenAI-compat. Separate design doc.
- Trace analytics over delegation spans (Phase 9 originally).
- Tuning loop that proposes skill patches from eval diffs (Phase 9+).

### Success Signal

Prompt and skill changes in PRs are accompanied by an automatically posted eval delta. Regressions on critical rubrics block merge. Human reviewers spend their time on the *why*, not the *whether*.