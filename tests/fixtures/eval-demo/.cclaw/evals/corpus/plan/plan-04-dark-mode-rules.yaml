id: plan-04-dark-mode-rules
stage: plan
input_prompt: |
  Audit the dark-mode plan for milestones/steps/risks coverage.
fixture: ./plan-01-dark-mode/fixture.md
expected:
  rules:
    must_contain:
      - "Milestones"
      - "Steps"
      - "Risks"
    must_not_contain:
      - "open question"
      - "TBD"
    regex_required:
      - pattern: "M\\d+"
        description: Milestone ids
      - pattern: "S\\d+"
        description: Step ids
    min_occurrences:
      "M1": 1
      "S1": 1
    unique_bullets_in_section:
      - Milestones
      - Steps
      - Risks
