id: spec-04-dark-mode-rules
stage: spec
input_prompt: |
  Audit the dark-mode spec for acceptance/interfaces/non-goals.
fixture: ./spec-01-dark-mode/fixture.md
expected:
  rules:
    must_contain:
      - "Acceptance Criteria"
      - "Interfaces"
      - "Non-Goals"
    must_not_contain:
      - "TBD"
      - "probably"
    regex_required:
      - pattern: "`(GET|POST|PATCH|PUT|DELETE) /"
        description: HTTP method + path reference
    unique_bullets_in_section:
      - Acceptance Criteria
      - Non-Goals
