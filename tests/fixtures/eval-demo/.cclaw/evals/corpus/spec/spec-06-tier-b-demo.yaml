id: spec-06-tier-b-demo
stage: spec
input_prompt: |
  Read the seeded README.md via the sandbox tools and produce a one-paragraph
  spec that captures the product's scope and one acceptance criterion.
context_files:
  - README.md
expected:
  judge:
    required_checks:
      - acceptance-criteria-coverage
