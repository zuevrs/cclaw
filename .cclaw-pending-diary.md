## 2026-05-11 — v8.30 — Skill anatomy gaps (When NOT + Common rationalizations)

**topic**: cclaw v8.30 — skill anatomy gaps

cclaw v8.30 closed two addy-anatomy gaps: `## When NOT to apply` was missing on 0/18 skills; the addy two-column rationalizations table was present on only 2/8 top skills.

Edited every one of the 18 skill markdown files to add a `## When NOT to apply` H2 with a 3-7-bullet negative-scope list (placement immediately after each skill's existing `## When to use`). Six of the top-8 skills (review-discipline, commit-hygiene, ac-discipline, api-evolution, debug-and-browser, triage-gate) gained a fresh `## Common rationalizations` H2 with an 8-row markdown table. The other two top-8 skills (tdd-and-verification, code-simplification) already shipped the table in v8.13 / v8.27 — left verbatim.

Two skills had pre-existing precursors that were extended in place:
- `tdd-and-verification` had `## When TDD does not apply` — renamed to `## When NOT to apply`, kept the bootstrap exception paragraph verbatim, added 4 more negative-scope bullets.
- `code-simplification` had an inline `**When NOT to use:**` bullet list inside `## When to use` — lifted to a top-level H2 verbatim.

Tripwire: tests/unit/v830-skill-anatomy-gaps.test.ts (8 tests, 3 describe blocks). AC-1 = every skill has `## When NOT to apply` with non-empty body. AC-2 = each top-8 skill has a `Common rationalizations` / `Anti-rationalization` H2 with a two-column markdown table. AC-3 = additive-only: H1 `# Skill: <name>` preserved, v8.26 `## When to use` heading preserved, body ≤1500 lines. v8.26 tripwire unmodified; both fire independently.

PR #259, all 9 CI checks green, squash-merged. 818 → 826 tests (+8 net), 58 → 59 files. Drop-in migration, no schema bump, no orchestrator behaviour change. `release:check` green.


## 2026-05-11 — v8.31 — Path-aware orchestrator trimming

**topic**: cclaw v8.31 — path-aware orchestrator trimming

cclaw v8.31 lifted two path-conditional sections out of the orchestrator body into new on-demand runbooks, extending the v8.22 gating pattern.

Created `runbooks/pause-resume.md` (full Hop 4 step/auto mechanics + Confidence × mode table + common-rules block; ~2.7K chars). Trigger: every stage exit when `triage.path != ["build"]` (non-inline pause). Created `runbooks/plan-small-medium.md` (ac-author dispatch contract for small-medium plan; pre-author research order, input list, output spec, slim-summary shape; ~1.7K chars). Trigger: `triage.complexity == "small-medium"` AND `plan` in `triage.path`.

Body shrank from 472 → 434 lines, 45212 → 41753 chars (~7.7% cut on every `/cc`). Per-path measurements after the lift:
- inline: 41753 chars (~10438 tokens) — body only, no dispatches
- small-medium: 69722 chars (~17431 tokens) — body + 7 runbooks
- large-risky: 84390 chars (~21098 tokens) — body + 10 runbooks

Strict ordering large-risky > small-medium > inline enforced by the new tripwire `tests/unit/v831-path-aware-trimming.test.ts` (25 tests, 5 describe blocks). Body keeps the orchestrator-wide invariants (`/cc` is the only resume verb, end-of-turn is the step pause, `Confidence: low` is a hard gate in both modes, `/cc-cancel` is never a clickable option) so the v8.11 / v8.14 / v8.21 / v8.22 / v8.24 / v8.26 / v8.30 tripwires stay green unchanged. v8.22 tripwire's hardcoded `expectedRunbookFiles` list grew 10 → 12 (extension, not relaxation).

PR #260, all 9 CI checks green, squash-merged. 826 → 851 tests (+25 net) across 59 → 60 files. `release:check` green.

Deferred to future slug: lifting the v8.13/v8.24 two-pass reviewer block (would require updating 14 v8.24 tripwire assertions), the SUMMARY_RETURN_EXAMPLE prose enum, the Hop 5 compound-gate signal list, and moving the per-path budget ceilings into a `config.pathBudget` knob.


## 2026-05-11 — v8.32 — Additive skills batch I (context-engineering + performance-optimization)

**topic**: cclaw v8.32 — additive skills batch I

cclaw v8.32 added two new addy-anatomy skills following the v8.27 `code-simplification` lift pattern.

`context-engineering.md` (~9.5K chars) is the canonical home for the rules behind the orchestrator's existing context-construction primitives: five-layer hierarchy (rules → specs → source → errors → conversation), three packing strategies (brain dump for inline / selective include for small-medium / hierarchical summary for large-risky), and three confusion-management sources (internal conflict / missing requirement / drift). Stage-windowed on `["always"]`.

`performance-optimization.md` (~12K chars) is the citation target for the reviewer's existing `perf` axis. Iron rule "don't optimise without numbers" + Core Web Vitals targets (LCP 2.5s / INP 200ms / CLS 0.1 at the Lighthouse Slow-4G+4×-CPU profile) + measurement-first workflow (baseline → RED → GREEN → REFACTOR) + N+1 anti-pattern catalogue (6 entries with concrete fixes) + bundle budget table (6 entries: Initial JS / Initial CSS / per-route chunk / image / font / total page weight). Stage-windowed on `["build", "review"]`; triggers include `touch-surface:ui` and `finding:perf`.

Both follow v8.26 anatomy (Overview / When to use / When NOT to apply / ≥2 depth sections / Common rationalizations / Red flags / Verification / Cross-references) and cclaw's two-column `excuse → truth` rationalizations table shape. AUTO_TRIGGER_SKILLS grew 18 → 20. v8.27 tripwire's hardcoded count updated to 20 / "20 skills total" (extension, not relaxation).

Tripwire `tests/unit/v832-additive-skills-batch-1.test.ts` (21 tests, 5 describe blocks) pins registration, stage windowing, content presence, description-body sync, and v8.26 + v8.30 anatomy invariants on both new skills.

PR #261, all 9 CI checks green, squash-merged. 851 → 872 tests (+21 net) across 60 → 61 files. `release:check` green.

Deferred: envelope-review runbook (forward pointer in context-engineering); `cclaw.config.json > perfBudget` knob plumbing (documented but not wired through `src/config.ts`); reviewer `perf` axis prompt pointer swap; stage `["always"]` overlap audit (4-5 skills now share the always-window).


## 2026-05-11 — v8.33 — Additive skills batch II (frontend-ui-engineering + ci-cd-and-automation)

**topic**: cclaw v8.33 — additive skills batch II

cclaw v8.33 added the remaining two addy-anatomy skills the five-audit review picked, completing the UI / ship boundary coverage.

`frontend-ui-engineering.md` (~13K chars) is the canonical citation target for UI-stage findings. Two component-architecture rules (composition over configuration / controlled vs uncontrolled — one mode per component). Design-system adherence: "use the system, don't invent tokens" with a 3-row situation/do/don't table. 10-entry AI-aesthetic anti-pattern table (purple gradient hero / rounded-2xl everywhere / oversized padding / center-everything / drop-shadow-on-everything / soft-white-on-soft-white / lucide-icon-on-every-line / animations-on-every-state / generic placeholder text / sentence-case button labels — each with the "what to do instead" right column). WCAG 2.1 AA baseline broken into 5 subsections: focus + keyboard / semantic HTML / ARIA-only-when-needed / colour + contrast (4.5:1 text, 3:1 non-text) / motion + timing. Responsive design: mobile-first + fluid before fixed + ≥44×44px touch targets, with a 4-row breakpoint table. Stage-windowed on `["build", "review"]`; triggers include `touch-surface:ui`, `.tsx`/`.jsx`/`.vue`/`.svelte`/`.html`/`.css` file patterns, `finding:readability:ui`, `finding:accessibility`.

`ci-cd-and-automation.md` (~12K chars) is the canonical citation target for ship-stage CI work. 8-stage quality-gate ordering with time budgets (setup ≤60s → lint ≤30s → typecheck ≤60s → test ≤5min → coverage → security-audit → bundle-check → optional E2E). GitHub Actions baseline template (Node + TypeScript reference, ~50 lines of real YAML the user can drop in: `permissions: contents: read`, `concurrency` with cancel-in-progress, matrix Node 20/22, pinned `actions/checkout@v4` + `actions/setup-node@v4`). Three optimisation patterns: caching (6-row table with hit-rate + typical-save), parallelism (jobs-as-siblings + sharding when test runs > 5 min), path filters (skip CI on doc-only, per-package monorepo filters). Branch-protection essentials (9-row table covering review requirements, status checks, linear history, signed commits, push restrictions, no force pushes, no deletions). Stage-windowed on `["ship"]`; triggers include `stage:ship`, `.github/workflows`, `.gitlab-ci|azure-pipelines|Jenkinsfile`, `ac:ci`, `ac:cd`.

Both follow v8.26 anatomy + v8.30 invariants (Overview / When to use / When NOT to apply / ≥2 depth sections / Common rationalizations 8-row table / Red flags / Verification / Cross-references) and the cclaw two-column `excuse → truth` rationalizations shape. AUTO_TRIGGER_SKILLS grew 20 → 22.

Tripwire `tests/unit/v833-additive-skills-batch-2.test.ts` (26 tests, 5 describe blocks) pins registration, stage windowing, content presence, description-body sync, and v8.26 + v8.30 anatomy invariants on both new skills.

Four tripwire extensions (not relaxations): v8.27 `code-simplification` count `20 → 22` and "20 skills total" → "22 skills total"; v8.32's `.toBe(20)` relaxed to `.toBeGreaterThanOrEqual(20)` with explicit "next slug pins new count" note; v8.16 cleanup ceiling `[15, 20]` → `[15, 24]` (floor unchanged); `prompt-budgets.test.ts` reviewer budget 48000 → 50000 chars / 630 → 660 lines and security-reviewer 17500 → 19000 chars / 220 → 240 lines (absorbing the frontend-ui-engineering skill body that lands in the review-stage block).

PR #TBD, all 9 CI checks pending, squash-merge target. 872 → 898 tests (+26 net) across 61 → 62 files. `release:check` green (pack `cclaw-cli-8.33.0.tgz`, 331.5kB / 142 files).

Deferred: `frontend-ui-engineering.example.tsx` companion runnable example; per-stack CI template variants (Python / Go / Rust / Docker / monorepo) as appendix sections; promoting accessibility to a first-class eighth reviewer axis; CI-config diff-aware reviewer dispatch (auto-attach ship-stage skills when the diff is `.github/workflows/`).


## 2026-05-11 — v8.34 — Knowledge problemType field + mid-flight runMode toggle

**topic**: cclaw v8.34 — knowledge problemType + mid-flight runMode toggle

cclaw v8.34 landed two paired schema/UX additions from the five-audit review.

**Item 5 — `problemType` in `KnowledgeEntry`.** Added optional `problemType?: ProblemType | null` where `ProblemType = "bug" | "knowledge" | "decision" | "performance" | "refactor"`. New exported `PROBLEM_TYPES` const + `matchesProblemType` helper (back-compat rule: absent / null surfaces only under the `knowledge` filter — the prior implicit default before v8.34). `NearKnowledgeOptions` gained `problemType?: ProblemType`; `findNearKnowledge` filters before scoring. CLI gained `--type=<kind>` flag with help text + invalid-value error path; surface combines with existing `--tag` / `--surface` / `--all` / `--json`.

**Item 6 — Mid-flight `runMode` toggle.** Lifted the existing immutability rule from `runMode` only (`complexity` / `acMode` / `path` still immutable). New "Mid-flight runMode toggle (v8.34)" section in `start-command.ts` Hop 2 documents `/cc --mode=auto` / `/cc --mode=step` parse points, the persistence rule (patches `flow-state.json > triage.runMode`, sticks across `/cc` invocations), the inline-path rejection literal `inline path has no runMode`, and the flag's combinability with task text. Same content mirrored into `flow-resume.md` (the resume-time entry point). `triage-gate.md` updated to point at the toggle (rationalizations row + main immutability paragraph).

Tripwire `tests/unit/v834-knowledge-type-and-runmode-toggle.test.ts` (16 tests, 2 describe blocks) pins all six AC: PROBLEM_TYPES shape, round-trip + validation, filter behaviour, back-compat, orchestrator-body documentation, flow-resume documentation.

One tripwire extension (not relaxation): v8.31 path-aware trimming budgets bumped to absorb ~1.9K chars of v8.34 toggle documentation (orchestrator-wide, not path-conditional, so it stays in body). Char ceiling 42500 → 44500; line ceiling 435 → 450; v8.30 ratio ceiling 0.95 → 0.97. Body is still meaningfully smaller than v8.30 baseline (45212).

Backward-compat preserved everywhere: `flow-state.json` schema unchanged (v8.34 uses an existing field); `knowledge.jsonl` schema is forward-compatible without a bump (absent `problemType` defaults to "knowledge"). No migration required.

PR #TBD, all 9 CI checks pending, squash-merge target. 898 → 914 tests (+16 net) across 62 → 63 files. `release:check` green (pack `cclaw-cli-8.34.0.tgz`, 334.7kB / 142 files).

Deferred: compound capture's auto-stamping of `problemType` from flow signals (v8.34 documents the rules; v8.35+ wires them); interactive multi-filter picker for TUI Browse-Knowledge menu; `/cc -a` / `/cc -s` short forms for the toggle (skipped because of collision with `-h` / `-v`); reviewer specialist prompts noticing `problemType` for hard-gate routing (e.g. perf-bug prior → reviewer perf axis becomes a `required` finding).


## 2026-05-11 — v8.35 — Project domain glossary (CONTEXT.md)

**topic**: cclaw v8.35 — CONTEXT.md project domain glossary (mattpocock pattern)

cclaw v8.35 landed item 8 of the five-audit review: an optional project-root `CONTEXT.md` file every flow-stage specialist reads at the start of dispatch as shared project vocabulary.

**Surface.** New module `src/context-glossary.ts` (~85 lines) exports `CONTEXT_MD_FILE_NAME = "CONTEXT.md"`, `contextGlossaryPath(projectRoot)`, `readContextGlossary(projectRoot)` (returns `null` on ENOENT, body on success; other errors rethrow), and `CONTEXT_MD_TEMPLATE` — a stub with H1 + marker comment `<!-- cclaw-context: stub -->` + 5 canonical H2s (Slug / Acceptance Criterion / Compound capture / Runbook / Triage) + trailing "Project-specific terms" footer.

**Install layer.** New `--with-context` flag opt-in writes the stub when missing. Default install never touches CONTEXT.md (the file is opt-in user vocabulary; cclaw doesn't surprise the user with a project-root markdown file they never asked for). Existing files are preserved verbatim regardless of the flag (`maybeWriteContextStub` returns one of `"created" | "exists" | "skipped"`; the install emits a "Preserved CONTEXT.md" progress event when the file was already there).

**Specialist prompts.** Four flow-stage specialists (`design.ts` / `ac-author.ts` / `slice-builder.ts` / `reviewer.ts`) each gained one bullet in their input lists naming CONTEXT.md, guarded with "if it exists" / "missing file is a no-op" so absence never breaks a dispatch. `security-reviewer.ts` intentionally untouched — CVE detection doesn't depend on project-domain vocabulary.

Tripwire `tests/unit/v835-context-md-glossary.test.ts` (18 tests, 4 describe blocks) pins all six AC: template shape (>400 chars, H1 + marker + 3 canonical H2s + project-specific-terms instruction), `readContextGlossary` semantics (null on missing, body on present, graceful on garbage), per-specialist `CONTEXT.md` mention with guard phrasing, path / filename literals, marker comment as future-refresh anchor, install opt-in (no-flag does NOT create; `withContext: true` creates the canonical stub; `withContext: true` preserves a user-authored file verbatim).

No existing tripwires changed. Skill anatomy (v8.26 / v8.30), path-aware orchestrator trimming (v8.31), skill counts (v8.32 / v8.33), and knowledge / runMode toggle (v8.34) all green.

PR #264, merge OID `5f48eb3`, all 9 CI checks green, squash-merged with branch deletion. 914 → 932 tests (+18 net) across 63 → 64 files. `release:check` green (pack `cclaw-cli-8.35.0.tgz`, 339.8 kB / 144 files).

Deferred: TUI prompt at `cclaw install` (the single-shot main-menu / harness-prompt layer would need a new state machine; the `--with-context` CLI flag is the operator escape hatch); compound-capture step reading CONTEXT.md (edit surface intersects v8.34's `problemType` work); `cclaw context refresh` / `cclaw context diff` subcommand to refresh shipped sections without clobbering user terms (marker comment is in place as the anchor); security-reviewer reading CONTEXT.md (intentionally skipped); AGENTS.md / CLAUDE.md cross-references nudging non-cclaw rigs to read the file (out of scope for an additive cclaw slug).


## 2026-05-12 — v8.36 — is_behavior_adding predicate + posture field

**topic**: cclaw v8.36 — is_behavior_adding predicate + posture field

cclaw v8.36 closed the "Iron Law structurally absent" gap by combining two reference patterns: the gsd-v1 `is_behavior_adding(touchSurface)` file-extension predicate (the gate, lives in `commit-helper.mjs`) and the everyinc-compound `posture` per-AC frontmatter field (the annotation, lives in `plan.md`). The 5-bullet prose list `## When NOT to apply` in `tdd-and-verification.md` was replaced by a 6-row machine-readable mapping table; the slice-builder no longer re-derives the right ceremony from a verbal heuristic — it picks a row.

**Surface.** New module `src/is-behavior-adding.ts` (~70 LOC, pure function + canonical exclusion description constant). New `POSTURES = ["test-first", "characterization-first", "tests-as-deliverable", "refactor-only", "docs-only", "bootstrap"]` const tuple, `Posture` type, `DEFAULT_POSTURE = "test-first"`, `isPosture()` type guard, `AcceptanceCriterionState.posture?` in `src/types.ts`. `parseArtifact()` validates the field — unknown values throw `FrontmatterError` citing AC id + bad value + allowed list. `PLAN_TEMPLATE` ships `posture: test-first` on every AC stanza + `posture` column in the AC table + documentation bullet listing all six values.

**Hook.** `commit-helper.mjs` (template-literal body in `src/content/node-hooks.ts`) inlines the predicate verbatim (no shared imports; hook runs as standalone `.mjs`). `ALLOWED_PHASES` extended to `red | green | refactor | test | docs`. Posture resolves per AC with legacy `buildProfile === "bootstrap"` promoted into `posture: bootstrap`. Posture-specific phase routing: `test-first` / `characterization-first` keep the full RED → GREEN → REFACTOR chain; `tests-as-deliverable` accepts a single `--phase=test` (recorded under `phases.green`); `refactor-only` accepts `--phase=refactor` directly (no RED required); `docs-only` accepts `--phase=docs` AND refuses if `isBehaviorAdding(touchSurface)` returns `true` (the predicate-as-double-check); `bootstrap` skips RED for AC-1 only. All existing strict-mode invariants preserved (git-unavailable hard fail, RED stages no production files, AC id required).

**Prompts.** `ac-author.ts` gained `## Posture heuristic table (v8.36; mandatory)` mapping AC verbs → posture (contract test → `tests-as-deliverable`, rename/extract → `refactor-only`, docs/ADR/README → `docs-only`, test framework setup → `bootstrap`, characterization pin → `characterization-first`, default → `test-first`); AC table column added; Phase 7 self-review item 16 ("posture set on every AC, consistent with touchSurface"). `slice-builder.ts` gained `## Posture-driven ceremony (v8.36, strict mode)` with one paragraph per posture explaining the `--phase` flag, commit count, and the stop-and-surface rule. `reviewer.ts` gained `## Posture-aware TDD checks (v8.36, strict mode)` — A-1 "missing RED" finding now conditional on posture; replaced by per-posture deliverable / no-behaviour-delta / docs checks.

**Skill.** `tdd-and-verification.md` got `## Posture mapping (v8.36, supersedes "When NOT to apply")` — the canonical 6-row table (posture → ceremony → commit-helper invocation → verification-loop mode → reviewer checks) — plus the 5 legacy prose exceptions kept as worked examples. New minimal `## When NOT to apply` covers the two cases outside the posture system (inline acMode, discovery-phase artifacts) so v8.30 skill anatomy stays green.

Tripwires (3 new files, 41 new tests): `tests/unit/is-behavior-adding.test.ts` (12 — predicate cases), `tests/unit/posture.test.ts` (12 — enum + parser + template), `tests/unit/v836-cleanup.test.ts` (17 — cross-surface alignment for predicate + hook body + prompts + skill). `tests/unit/prompt-budgets.test.ts` budgets bumped for ac-author (46K → 56K chars, 17% headroom) and reviewer (50K → 62K chars, 19% headroom); lines unchanged.

PR pending. 932 → 973 tests (+41 net) across 64 → 67 files; all 67 files green at commit time. `npm run build` exit 0, `npm test` 973/973. Migration: existing plans (no posture) default to `test-first`; existing `build_profile: bootstrap` still recognised.

Deferred: posture introduced in TUI / cclaw-meta (currently text-only in plan.md); `cclaw lint` / `cclaw doctor` subcommand to dry-run the predicate against an in-flight plan + flag the contradictions before commit; ac-author "posture override prompt" when the verb suggests one posture but `touchSurface` suggests another (currently the agent just self-corrects; a structured surface would prevent silent drift); legacy `buildProfile` field deprecation timeline (still honoured in v8.36; remove in v9 or v10 with a 2-version warning window).


## 2026-05-12 — v8.37 — CLI consolidation (non-interactive install is the single installer)

**topic**: cclaw v8.37 — non-interactive CLI surface consolidation

cclaw v8.37 collapsed `cclaw --non-interactive sync` and `cclaw --non-interactive upgrade` into `cclaw --non-interactive install`. All three previously called the same idempotent installer with orphan cleanup under the hood — `syncCclaw()` and `upgradeCclaw()` were thin aliases. In the TUI those names map to three menu rows for intent-named UX (a human menu reader benefits from "Install" vs "Sync" vs "Upgrade"); in CI the rename was cognitive overhead with zero functional difference.

**Surface.** `src/cli.ts` gained two new constants: `COLLAPSED_NON_INTERACTIVE_COMMANDS` (a record mapping retired commands to migration hints — `sync` and `upgrade` point at `install`), and `NON_INTERACTIVE_COMMANDS` (a Set enumerating the supported five: `install`, `knowledge`, `uninstall`, `version`, `help`). The dispatch routes `--non-interactive sync` / `upgrade` to the migration hint + exit 1 BEFORE the menu-action lookup; the menu-action switch handles only `install` / `uninstall` / `knowledge` / `version`. `help` got hoisted above the bare-subcommand gate so both `cclaw help` and `cclaw --non-interactive help` print the help screen and exit 0 (read-only command that doesn't benefit from the TUI / non-interactive distinction).

**Exit codes.** Migration hint exits 1 (the invocation shape is valid, the command itself is retired). Unknown non-interactive command exits 2 (the invocation shape is wrong). Help / version exit 0. Install / knowledge / uninstall exit 0 on success.

**Help text + README.** `HELP_USAGE` unchanged; `HELP_NOTES` gained a `v8.37 migration:` paragraph naming the rename + rationale. `HELP_COMMANDS` rewritten to list the canonical five first, then the retired `sync` / `upgrade` framed as renamed with the migration target inline. README CLI section lists the five canonical commands + a prose paragraph explaining the consolidation + noting the TUI menu keeps its Sync / Upgrade rows.

Tripwire `tests/unit/cli-non-interactive.test.ts` (11 tests): install works (and is idempotent on repeat-call), sync exits 1 with migration message, upgrade exits 1 with migration message, sync/upgrade do NOT call `syncCclaw` / `upgradeCclaw` internally (verified by checking `.cclaw/config.yaml` does NOT appear after the call on a virgin project), knowledge works, uninstall works, version prints the version string, help prints the help screen, `init` non-interactive alias still works for backward compat, help text lists the new canonical surface. `tests/unit/cli.test.ts` legacy sync test replaced with two new tests (migration assertion + install-is-idempotent assertion).

PR pending. 973 → 985 tests (+12 net = 11 new + 1 legacy test split into 2). `npm run build` exit 0, `npm test` 985/985.

Deferred: removing `sync` / `upgrade` from `SUBCOMMAND_TO_MENU_ACTION` map entirely (kept for TUI menu dispatch — removing would also remove the TUI menu rows, which is the opposite of the intent); deprecation timeline for the migration hint itself (printed by the hint indefinitely; could become a hard rejection in v9 once CI rollovers complete); `--non-interactive sync --strict` opt-in for scripts that want the explicit "fail loud if not already installed" behaviour (currently install handles both first-run and re-run silently; some operators might want a strict-mode flag).

